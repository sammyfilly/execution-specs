:mod:`ethereum.frontier.vm.instructions.arithmetic`
===================================================

.. py:module:: ethereum.frontier.vm.instructions.arithmetic

Ethereum Virtual Machine (EVM) Arithmetic Instructions
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.. contents:: Table of Contents
    :backlinks: none
    :local:

Introduction
------------

Implementations of the EVM Arithmetic instructions.


Module Contents
---------------


Functions
~~~~~~~~~

.. autoapisummary::
   :nosignatures:

   ethereum.frontier.vm.instructions.arithmetic.add
   ethereum.frontier.vm.instructions.arithmetic.sub
   ethereum.frontier.vm.instructions.arithmetic.mul
   ethereum.frontier.vm.instructions.arithmetic.div
   ethereum.frontier.vm.instructions.arithmetic.sdiv
   ethereum.frontier.vm.instructions.arithmetic.mod
   ethereum.frontier.vm.instructions.arithmetic.smod
   ethereum.frontier.vm.instructions.arithmetic.addmod
   ethereum.frontier.vm.instructions.arithmetic.mulmod
   ethereum.frontier.vm.instructions.arithmetic.exp
   ethereum.frontier.vm.instructions.arithmetic.signextend



Module Details
---------------

add
~~~


.. function:: add(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Adds the top two elements of the stack together, and pushes the result back
   on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than `3`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 29-52

sub
~~~


.. function:: sub(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Subtracts the top two elements of the stack, and pushes the result back
   on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than `3`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 55-78

mul
~~~


.. function:: mul(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Multiply the top two elements of the stack, and pushes the result back
   on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than `5`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 81-104

div
~~~


.. function:: div(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Integer division of the top two elements of the stack. Pushes the result
   back on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than `5`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 107-133

sdiv
~~~~


.. function:: sdiv(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Signed integer division of the top two elements of the stack. Pushes the
   result back on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than `5`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 136-166

mod
~~~


.. function:: mod(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Modulo remainder of the top two elements of the stack. Pushes the result
   back on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than `5`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 169-195

smod
~~~~


.. function:: smod(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Signed modulo remainder of the top two elements of the stack. Pushes the
   result back on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than `5`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 198-225

addmod
~~~~~~


.. function:: addmod(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Modulo addition of the top 2 elements with the 3rd element. Pushes the
   result back on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `3`.
   :raises OutOfGasError: If `evm.gas_left` is less than `8`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 228-256

mulmod
~~~~~~


.. function:: mulmod(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Modulo multiplication of the top 2 elements with the 3rd element. Pushes
   the result back on the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `3`.
   :raises OutOfGasError: If `evm.gas_left` is less than `8`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 259-287

exp
~~~


.. function:: exp(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Exponential operation of the top 2 elements. Pushes the result back on
   the stack.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 290-319

signextend
~~~~~~~~~~


.. function:: signextend(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Sign extend operation. In other words, extend a signed number which
   fits in N bytes to 32 bytes.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than `5`.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/arithmetic.py
   :language: python
   :lines: 322-364
