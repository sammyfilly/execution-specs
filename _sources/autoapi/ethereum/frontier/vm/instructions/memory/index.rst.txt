:mod:`ethereum.frontier.vm.instructions.memory`
===============================================

.. py:module:: ethereum.frontier.vm.instructions.memory

Ethereum Virtual Machine (EVM) Memory Instructions
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.. contents:: Table of Contents
    :backlinks: none
    :local:

Introduction
------------

Implementations of the EVM Memory instructions.


Module Contents
---------------


Functions
~~~~~~~~~

.. autoapisummary::
   :nosignatures:

   ethereum.frontier.vm.instructions.memory.mstore
   ethereum.frontier.vm.instructions.memory.mstore8
   ethereum.frontier.vm.instructions.memory.mload
   ethereum.frontier.vm.instructions.memory.msize



Module Details
---------------

mstore
~~~~~~


.. function:: mstore(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Stores a word to memory.
   This also expands the memory, if the memory is
   insufficient to store the word.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than
       `3` + gas needed to extend memeory.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/memory.py
   :language: python
   :lines: 29-64

mstore8
~~~~~~~


.. function:: mstore8(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Stores a byte to memory.
   This also expands the memory, if the memory is
   insufficient to store the word.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `2`.
   :raises OutOfGasError: If `evm.gas_left` is less than
       `3` + gas needed to extend memory.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/memory.py
   :language: python
   :lines: 67-104

mload
~~~~~


.. function:: mload(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Load word from memory.

   :param evm: The current EVM frame.

   :raises StackUnderflowError: If `len(stack)` is less than `1`.
   :raises OutOfGasError: If `evm.gas_left` is less than
       `3` + gas needed to extend memory.




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/memory.py
   :language: python
   :lines: 107-142

msize
~~~~~


.. function:: msize(evm: ethereum.frontier.vm.Evm) -> None
   :noindexentry:

   Push the size of active memory in bytes onto the stack.

   :param evm: The current EVM frame.

   :raises OutOfGasError: If `evm.gas_left` is less than `2`




.. undocinclude:: /../src/ethereum/frontier/vm/instructions/memory.py
   :language: python
   :lines: 145-161
