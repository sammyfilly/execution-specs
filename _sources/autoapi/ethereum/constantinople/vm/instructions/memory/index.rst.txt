:mod:`ethereum.constantinople.vm.instructions.memory`
=====================================================

.. py:module:: ethereum.constantinople.vm.instructions.memory

Ethereum Virtual Machine (EVM) Memory Instructions
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.. contents:: Table of Contents
    :backlinks: none
    :local:

Introduction
------------

Implementations of the EVM Memory instructions.


Module Contents
---------------


Functions
~~~~~~~~~

.. autoapisummary::
   :nosignatures:

   ethereum.constantinople.vm.instructions.memory.mstore
   ethereum.constantinople.vm.instructions.memory.mstore8
   ethereum.constantinople.vm.instructions.memory.mload
   ethereum.constantinople.vm.instructions.memory.msize



Module Details
---------------

mstore
~~~~~~


.. function:: mstore(evm: ethereum.constantinople.vm.Evm) -> None
   :noindexentry:

   Stores a word to memory.
   This also expands the memory, if the memory is
   insufficient to store the word.

   :param evm: The current EVM frame.

   :raises ~ethereum.constantinople.vm.exceptions.StackUnderflowError: If `len(stack)` is less than `2`.
   :raises ~ethereum.constantinople.vm.exceptions.OutOfGasError: If `evm.gas_left` is less than
       `3` + gas needed to extend memeory.




.. undocinclude:: /../src/ethereum/constantinople/vm/instructions/memory.py
   :language: python
   :pyobject: mstore

mstore8
~~~~~~~


.. function:: mstore8(evm: ethereum.constantinople.vm.Evm) -> None
   :noindexentry:

   Stores a byte to memory.
   This also expands the memory, if the memory is
   insufficient to store the word.

   :param evm: The current EVM frame.

   :raises ~ethereum.constantinople.vm.exceptions.StackUnderflowError: If `len(stack)` is less than `2`.
   :raises ~ethereum.constantinople.vm.exceptions.OutOfGasError: If `evm.gas_left` is less than
       `3` + gas needed to extend memory.




.. undocinclude:: /../src/ethereum/constantinople/vm/instructions/memory.py
   :language: python
   :pyobject: mstore8

mload
~~~~~


.. function:: mload(evm: ethereum.constantinople.vm.Evm) -> None
   :noindexentry:

   Load word from memory.

   :param evm: The current EVM frame.

   :raises ~ethereum.constantinople.vm.exceptions.StackUnderflowError: If `len(stack)` is less than `1`.
   :raises ~ethereum.constantinople.vm.exceptions.OutOfGasError: If `evm.gas_left` is less than
       `3` + gas needed to extend memory.




.. undocinclude:: /../src/ethereum/constantinople/vm/instructions/memory.py
   :language: python
   :pyobject: mload

msize
~~~~~


.. function:: msize(evm: ethereum.constantinople.vm.Evm) -> None
   :noindexentry:

   Push the size of active memory in bytes onto the stack.

   :param evm: The current EVM frame.

   :raises ~ethereum.constantinople.vm.exceptions.OutOfGasError: If `evm.gas_left` is less than `2`




.. undocinclude:: /../src/ethereum/constantinople/vm/instructions/memory.py
   :language: python
   :pyobject: msize
